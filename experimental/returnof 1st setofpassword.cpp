                      //FILE NOT IN USE



/*#include<iostream>                  //this file will generate random numbers btw 97 and 122
#include<fstream>                   //convert them into ASCII
#include"Header.h"                  //store it in string and will return it to password creater
#include<ctime>
using namespace std;


string returnpassword(int psdlng) {
	 int p; int strlng; char C; string G; string F;
	srand(time(0));
	//F = "";
	for (int i = 0; i < psdlng; i++) {                //this loop is generating one password of the specified length i.e. psdlng
		 int x = (rand() % 22 + 97);
		char z = int(x);
		F += z;
		//strlng = F.length();
	}
	//cout << F << endl;

	return F;                                      //returning the password to the parent file
	
}*/